import 'package:flutter/material.dart';

/// Pantalla de detalle de captación (placeholder)
class PropertyListingDetailScreen extends StatelessWidget {
  const PropertyListingDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Detalle de Captación'),
      ),
      body: const Center(
        child: Text('Pantalla de detalle (en desarrollo)'),
      ),
    );
  }
}
